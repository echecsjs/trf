// Column offsets of each field within a 001 player line (0-indexed).
// 1-based column = offset + 1.

const COL_BIRTH_DATE = 70;
const COL_FEDERATION = 53;
const COL_FIDE_ID = 57;
const COL_NAME = 14;
const COL_PAIRING_NUMBER = 4;
const COL_POINTS = 80;
const COL_RANK = 84;
const COL_RATING = 48;
const COL_SEX = 9;
const COL_TITLE = 10;
const ROUND_ENTRY_LENGTH = 10;
const ROUND_RESULTS_OFFSET = 91;

export {
  COL_BIRTH_DATE,
  COL_FEDERATION,
  COL_FIDE_ID,
  COL_NAME,
  COL_PAIRING_NUMBER,
  COL_POINTS,
  COL_RANK,
  COL_RATING,
  COL_SEX,
  COL_TITLE,
  ROUND_ENTRY_LENGTH,
  ROUND_RESULTS_OFFSET,
};
