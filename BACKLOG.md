# Backlog

Last updated: 2026-03-30

## Medium

- [ ] Fix README `@echecs/swiss` interop example — uses stale API (`dutch`
      instead of `pair`, wrong field names `blackId`/`whiteId`, flat `Game[]`
      instead of `Game[][]`).

## Low

- [ ] Records 801/802 — Informative team round-by-round results. Requires
      structured parsing (multi-line records like 310). Not used by any pairing
      engine.
